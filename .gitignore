# Hide sensitive information 
config/*
!config/default.yml

# Hide ngrok (I have ngrok because Discord API doesn't allow http requests for the interactions..)
ngrok.exe

node_modules
package-lock.json

# exclude New Relic stuff i guess lol
newrelic.js
newrelic_agent.log